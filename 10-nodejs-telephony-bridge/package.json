{
  "name": "silk-nodejs-telephony-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic Node.js bridge between carrier audio streams (Plivo, Exotel, raw PCM / SIP) and Silk's realtime socket, with dynamic resampling and codec conversion.",
  "scripts": {
    "start": "node bridge.js",
    "dev": "node --watch bridge.js",
    "mic": "node local-mic.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "node-record-lpcm16": "^1.0.1",
    "speaker": "^0.5.5"
  },
  "engines": {
    "node": ">=18"
  }
}
