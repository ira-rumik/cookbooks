websockets>=12
httpx>=0.27
python-dotenv>=1.0
numpy>=1.26
gpiozero>=2.0
# On Raspberry Pi OS install the system package instead of building:  sudo apt install python3-pyaudio
pyaudio>=0.2.14
# --- optional: WebRTC path (webrtc_agent.py) ---
# livekit>=1.0
# sounddevice>=0.4.7
