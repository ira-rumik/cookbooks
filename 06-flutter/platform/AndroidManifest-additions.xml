<!-- Add to android/app/src/main/AndroidManifest.xml, before <application>. -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
<!-- Only if you ever enable the camera. -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.microphone" android:required="true" />

<!--
  Dev only: allow plain http:// to your local token server. Remove for release
  or use an https tunnel (ngrok). Goes on the <application> element:
      android:usesCleartextTraffic="true"
-->
