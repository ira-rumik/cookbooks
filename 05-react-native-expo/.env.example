# EXPO_PUBLIC_ variables are bundled into the app — put only the token URL here, never the API key.
# iOS simulator: localhost works. Android emulator: 10.0.2.2. Physical device: LAN IP or an ngrok https URL.
EXPO_PUBLIC_TOKEN_URL=http://localhost:8787/api/token
